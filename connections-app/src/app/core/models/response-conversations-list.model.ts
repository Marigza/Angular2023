/* eslint-disable @typescript-eslint/naming-convention */
import { ConversationParams } from './conversation-params.model';

export interface ResponseConversationsList {
  Count: number;
  Items: ConversationParams[];
}

/* eslint-enable @typescript-eslint/naming-convention */
