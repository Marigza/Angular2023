/* eslint-disable @typescript-eslint/naming-convention */
import { PeopleParams } from './people-params.model';

export interface ResponsePeople {
  Count: number;
  Items: PeopleParams[];
}

/* eslint-enable @typescript-eslint/naming-convention */
