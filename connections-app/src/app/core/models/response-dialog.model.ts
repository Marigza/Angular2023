/* eslint-disable @typescript-eslint/naming-convention */
import { MessageParams } from './message-params.model';

export interface ResponseDialog {
  Count: number;
  Items: MessageParams[];
}

/* eslint-enable @typescript-eslint/naming-convention */
