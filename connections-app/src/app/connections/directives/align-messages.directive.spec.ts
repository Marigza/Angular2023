import { AlignMessagesDirective } from './align-messages.directive';

describe('AlignMessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignMessagesDirective();
    expect(directive).toBeTruthy();
  });
});
